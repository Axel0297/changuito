node_modules/
publico/

# El dataset se regenera con `npm run etl`; no tiene sentido versionar 10 MB
# que cambian todos los dias.
data/*.json
data/*.json.gz

# OJO: movil/assets/dataset.json NO va ignorado a proposito.
# Es la copia que viaja dentro de la app y EAS arma el paquete del build desde
# git: si git lo ignora, el build falla con "Unable to resolve module
# ../assets/dataset.json". Se versiona y se actualiza cuando se compila.

.expo/
dist/
web-build/
*.log
.DS_Store

# GitHub CLI portable, descargado para crear el repo
.herramientas/
